---
import HeroSection from '../sections/HeroSection.astro';
import CtaSection from '../sections/CtaSection.astro';

interface Props {
  label?: string;
  headline: string;
  description?: string;
  bodyHtml?: string;
  ctaTitle?: string;
  ctaDescription?: string;
  ctaButtonLabel?: string;
  ctaButtonUrl?: string;
  ctaSecondaryLabel?: string;
  ctaSecondaryUrl?: string;
}

const { label, headline, description, bodyHtml, ctaTitle, ctaDescription, ctaButtonLabel, ctaButtonUrl, ctaSecondaryLabel, ctaSecondaryUrl } = Astro.props;
---

<HeroSection label={label} headline={headline} description={description} narrow tightPadding />

{bodyHtml && (
  <section class="mx-auto max-w-3xl px-4 pb-24 sm:px-6 lg:px-12 text-left">
    <div class="dynamic-body w-full" set:html={bodyHtml} />
  </section>
)}

<CtaSection title={ctaTitle} description={ctaDescription} buttonLabel={ctaButtonLabel} buttonUrl={ctaButtonUrl} secondaryLabel={ctaSecondaryLabel} secondaryUrl={ctaSecondaryUrl} />
